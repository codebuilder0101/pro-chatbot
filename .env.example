# Copy to .env and fill in real values. NEVER commit the real .env.

# Supabase
SUPABASE_PROJECT_ID=""
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""   # server-only secret — keep out of git

VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# OpenAI — chatbot replies + text-to-speech
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"

# TronGrid — USDT (TRC-20) payment verification (optional; raises rate limits)
TRONGRID_API_KEY=""
